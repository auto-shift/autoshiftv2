apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ include "bootstrap.acm.namespace" . }}
  namespace: {{ include "bootstrap.acm.namespace" . }}
  annotations:
    helm.sh/hook-weight: "1"
spec:
  targetNamespaces:
  - {{ include "bootstrap.acm.namespace" . }}
