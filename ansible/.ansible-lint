---
# ansible-lint configuration
profile: production

# kubernetes.core modules use dynamic argument specs that ansible-lint
# cannot introspect. This is a known upstream limitation in the collection,
# not a code issue. The args[module] rule is experimental.
warn_list:
  - args[module]
