# Values with hub template overrides for per-cluster configuration
# These override the defaults in capabilities/openshift-dns/values.yaml
# No hub templates needed - DNS tolerations are static configuration

policy_namespace: ${POLICY_NAMESPACE}
