---
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: placement-policy-kiali
  namespace: ${POLICY_NAMESPACE}
spec:
  clusterSets:
${CLUSTER_SETS}
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchExpressions:
            - key: 'autoshift.io/kiali'
              operator: In
              values:
                - 'true'
