---
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: placement-policy-dev-spaces
  namespace: ${POLICY_NAMESPACE}
spec:
  clusterSets:
${CLUSTER_SETS}
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchExpressions:
            - key: 'dev-spaces'
              operator: In
              values:
                - 'true'
