---
apiVersion: policy.open-cluster-management.io/v1
kind: PolicyGenerator
metadata:
  name: policy-openshift-compliance-operator

policyDefaults:
  namespace: ${POLICY_NAMESPACE}
  remediationAction: enforce
  severity: high
  consolidateManifests: false
  placement:
    placementName: placement-policy-openshift-compliance-operator

policies:
  - name: policy-openshift-compliance-operator
    manifests:
      - path: policy-manifests/openshift-compliance-operator
