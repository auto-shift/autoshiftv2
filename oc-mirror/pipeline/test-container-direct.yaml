apiVersion: v1
kind: Pod
metadata:
  name: test-container-direct
  namespace: oc-mirror-pipeline
spec:
  serviceAccountName: oc-mirror-pipeline
  containers:
  - name: test
    image: registry-quay-quay-enterprise.apps.cluster-dlnph.dlnph.sandbox1297.opentlc.com/quayadmin/oc-mirror-autoshift:latest
    command: ["/bin/bash"]
    args: ["-c", "echo 'Container started'; pwd; ls -la /workspace/; echo 'Looking for autoshift-entrypoint.sh:'; find /workspace -name '*entrypoint*' 2>/dev/null || echo 'Not found'; sleep 30"]
  restartPolicy: Never