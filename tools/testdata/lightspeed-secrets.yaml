# Stub for the hub-side LLM API token that policy-lightspeed-credentials copies onto selected
# clusters via fromSecret. In production a cluster admin creates this by hand in the hub namespace
# named by config.openshift-lightspeed.hubSecret.
# Low-entropy fake value (not gitleaks-allowlisted here).
apiVersion: v1
kind: Secret
metadata:
  name: llm-apitoken
  namespace: lightspeed-secrets
type: Opaque
data:
  # fake: base64 of "test-token"
  apitoken: dGVzdC10b2tlbg==
