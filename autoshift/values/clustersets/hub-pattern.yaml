# Pattern-mode overrides for hub clusterset
# When deploying via Validated Patterns, GitOps is managed by the pattern framework
# so the openshift-gitops policy should be disabled on the hub.
hubClusterSets:
  hub:
    labels:
      gitops: 'false'
