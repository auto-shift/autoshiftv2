# Test networking config for local-cluster (hub) to validate nmstate NNCP generation
clusters:
  local-cluster:
    config:
      networking:
        interfaces:
          test-bond:
            type: bond
            name: bond99
            mode: active-backup
            ports:
              - dummy0
              - dummy1
            ipv4: disabled
            ipv6: disabled
