clusters:
  local-cluster:
    config:
      tssc-apps:
        dev:
          sample-app:
            environments: [ci, dev]
            # git-host: 'https://github.com'      # omit to auto-derive on-cluster GitLab URL
            # git-provider: 'gitlab'              # 'gitlab' (default) or 'github'
