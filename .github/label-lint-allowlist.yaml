# Label lint allowlist — keys exempted from CI contract enforcement.
#
# missing_ok: labels consumed by policy templates but intentionally absent
#   from _example*.yaml (e.g. internal-only labels, out-of-band provisioning).
#
# orphaned_ok: labels declared in _example*.yaml but not yet consumed by any
#   policy template (e.g. labels reserved for future use).
missing_ok: []
orphaned_ok: []
