# Allowlist for `autoshift-ci lint-labels`.
#
# Two buckets:
#   missing_ok:  labels consumed by templates that don't need an _example entry
#   orphaned_ok: labels in _example files that no template consumes

missing_ok: []
orphaned_ok: []
