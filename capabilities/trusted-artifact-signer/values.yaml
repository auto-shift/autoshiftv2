# Default values for trusted-artifact-signer
# Portable Helm chart - works with or without ACM

rhtasOperator:
  name: rhtas-operator
  namespace: openshift-operators
  channel: stable
  source: redhat-operators
  sourceNamespace: openshift-marketplace
