{{- $policyName := "policy-nvidia-gpu-config" }}
{{- $placementName := "placement-policy-nvidia-gpu-config" }}

apiVersion: policy.open-cluster-management.io/v1
kind: Policy
metadata:
  name: {{ $policyName }}
  namespace: {{ .Values.policy_namespace }}
  annotations:
    policy.open-cluster-management.io/standards: NIST SP 800-53
    policy.open-cluster-management.io/categories: CM Configuration Management
    policy.open-cluster-management.io/controls: CM-2 Baseline Configuration
spec:
  disabled: false
  # Wait for NVIDIA GPU operator to be installed first
  dependencies:
    - name: policy-nvidia-gpu-operator-install
      namespace: {{ .Values.policy_namespace }}
      apiVersion: policy.open-cluster-management.io/v1
      compliance: Compliant
      kind: Policy
  policy-templates:
    # ClusterPolicy - configures the GPU operator
    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: nvidia-gpu-cluster-policy
        spec:
          remediationAction: enforce
          severity: high
          object-templates:
            - complianceType: musthave
              objectDefinition:
                apiVersion: nvidia.com/v1
                kind: ClusterPolicy
                metadata:
                  name: {{ .Values.nvidiaGpu.clusterPolicy.name }}
                spec:
                  operator:
                    defaultRuntime: crio
                    use_ocp_driver_toolkit: '{{ "{{hub" }} index .ManagedClusterLabels "autoshift.io/nvidia-gpu-driver-toolkit" | default "{{ .Values.nvidiaGpu.clusterPolicy.driver.useOpenShiftDriverToolkit }}" {{ "hub}}" }}'
                    upgradeCRD: true
                  driver:
                    enabled: '{{ "{{hub" }} index .ManagedClusterLabels "autoshift.io/nvidia-gpu-driver" | default "{{ .Values.nvidiaGpu.clusterPolicy.driver.enabled }}" {{ "hub}}" }}'
                    upgradePolicy:
                      autoUpgrade: true
                      maxParallelUpgrades: 1
                      maxUnavailable: "25%"
                      podDeletion:
                        deleteEmptyDir: false
                        force: false
                        timeoutSeconds: 300
                      waitForCompletion:
                        timeoutSeconds: 0
                    repoConfig:
                      configMapName: ""
                    certConfig:
                      name: ""
                    licensingConfig:
                      nlsEnabled: false
                      configMapName: ""
                    virtualTopology:
                      config: ""
                    kernelModuleConfig:
                      name: ""
                  toolkit:
                    enabled: '{{ "{{hub" }} index .ManagedClusterLabels "autoshift.io/nvidia-gpu-toolkit" | default "{{ .Values.nvidiaGpu.clusterPolicy.toolkit.enabled }}" {{ "hub}}" }}'
                  devicePlugin:
                    enabled: '{{ "{{hub" }} index .ManagedClusterLabels "autoshift.io/nvidia-gpu-device-plugin" | default "{{ .Values.nvidiaGpu.clusterPolicy.devicePlugin.enabled }}" {{ "hub}}" }}'
                  dcgm:
                    enabled: '{{ "{{hub" }} index .ManagedClusterLabels "autoshift.io/nvidia-gpu-dcgm" | default "{{ .Values.nvidiaGpu.clusterPolicy.dcgmExporter.enabled }}" {{ "hub}}" }}'
                  dcgmExporter:
                    enabled: '{{ "{{hub" }} index .ManagedClusterLabels "autoshift.io/nvidia-gpu-dcgm" | default "{{ .Values.nvidiaGpu.clusterPolicy.dcgmExporter.enabled }}" {{ "hub}}" }}'
                    config:
                      name: ""
                    serviceMonitor:
                      enabled: true
                  gfd:
                    enabled: '{{ "{{hub" }} index .ManagedClusterLabels "autoshift.io/nvidia-gpu-gfd" | default "{{ .Values.nvidiaGpu.clusterPolicy.gfd.enabled }}" {{ "hub}}" }}'
                  migManager:
                    enabled: '{{ "{{hub" }} index .ManagedClusterLabels "autoshift.io/nvidia-gpu-mig" | default "{{ .Values.nvidiaGpu.clusterPolicy.migManager.enabled }}" {{ "hub}}" }}'
                  nodeStatusExporter:
                    enabled: true
                  mps:
                    enabled: '{{ "{{hub" }} index .ManagedClusterLabels "autoshift.io/nvidia-gpu-mps" | default "{{ .Values.nvidiaGpu.clusterPolicy.mps.enabled }}" {{ "hub}}" }}'
                  sandboxWorkloads:
                    enabled: '{{ "{{hub" }} index .ManagedClusterLabels "autoshift.io/nvidia-gpu-sandbox" | default "{{ .Values.nvidiaGpu.clusterPolicy.sandboxWorkloads.enabled }}" {{ "hub}}" }}'
                  validator:
                    plugin:
                      env:
                        - name: WITH_WORKLOAD
                          value: "false"
                  vgpuManager:
                    enabled: '{{ "{{hub" }} index .ManagedClusterLabels "autoshift.io/nvidia-gpu-vgpu" | default "{{ .Values.nvidiaGpu.clusterPolicy.vgpuManager.enabled }}" {{ "hub}}" }}'
                  vgpuDeviceManager:
                    enabled: '{{ "{{hub" }} index .ManagedClusterLabels "autoshift.io/nvidia-gpu-vgpu" | default "{{ .Values.nvidiaGpu.clusterPolicy.vgpuDeviceManager.enabled }}" {{ "hub}}" }}'
                  vfioManager:
                    enabled: true
                  sandboxDevicePlugin:
                    enabled: true
                  # Use external NFD if nvidia-gpu-nfd is false
                  nodeFeatureDiscovery:
                    enabled: '{{ "{{hub" }} index .ManagedClusterLabels "autoshift.io/nvidia-gpu-nfd" | default "{{ .Values.nvidiaGpu.clusterPolicy.nfd.enabled }}" {{ "hub}}" }}'
    # Status check - verify ClusterPolicy is ready
    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: nvidia-gpu-cluster-policy-status
        spec:
          remediationAction: inform
          severity: medium
          object-templates:
            - complianceType: musthave
              objectDefinition:
                apiVersion: nvidia.com/v1
                kind: ClusterPolicy
                metadata:
                  name: {{ .Values.nvidiaGpu.clusterPolicy.name }}
                status:
                  state: ready
---
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: {{ $placementName }}
  namespace: {{ .Values.policy_namespace }}
spec:
  clusterSets:
  {{- range $clusterSet, $value := $.Values.hubClusterSets }}
    - {{ $clusterSet }}
  {{- end }}
  {{- range $clusterSet, $value := $.Values.managedClusterSets }}
    - {{ $clusterSet }}
  {{- end }}
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchExpressions:
            - key: 'autoshift.io/nvidia-gpu'
              operator: In
              values:
              - 'true'
  tolerations:
    - key: cluster.open-cluster-management.io/unreachable
      operator: Exists
    - key: cluster.open-cluster-management.io/unavailable
      operator: Exists
---
apiVersion: policy.open-cluster-management.io/v1
kind: PlacementBinding
metadata:
  name: {{ $placementName }}
  namespace: {{ .Values.policy_namespace }}
placementRef:
  name: {{ $placementName }}
  apiGroup: cluster.open-cluster-management.io
  kind: Placement
subjects:
  - name: {{ $policyName }}
    apiGroup: policy.open-cluster-management.io
    kind: Policy

