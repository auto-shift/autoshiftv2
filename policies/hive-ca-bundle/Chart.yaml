apiVersion: v2
name: hive-ca-bundle
description: >
  Configures HiveConfig with additionalCertificateAuthoritiesSecretRef and deploys
  the user-ca-bundle Secret to each managed cluster's namespace on the hub.
  Required for importing clusters with self-signed certificates.
type: application
version: 0.0.1
appVersion: "0.0.1"
