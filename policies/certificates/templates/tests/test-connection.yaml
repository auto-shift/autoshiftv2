apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "certificates.fullname" . }}-test-connection"
  labels:
    {{- include "certificates.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "certificates.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
