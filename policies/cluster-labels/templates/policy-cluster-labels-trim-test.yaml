{{ if (.Values.debug | default false) }}
{{- $policyName := "policy-trim-marker-test" }}
{{- $policyNamespace := $.Values.policy_namespace }}
{{- $placementName := (printf "placement-%s" $policyName) }}
apiVersion: policy.open-cluster-management.io/v1
kind: Policy
metadata:
  name: {{ $policyName }}
  namespace: {{ $policyNamespace }}
  annotations:
    policy.open-cluster-management.io/categories: CM Configuration Management
    policy.open-cluster-management.io/controls: CM-2 Baseline Configuration
    policy.open-cluster-management.io/standards: NIST SP 800-53
spec:
  remediationAction: enforce
  disabled: false
  policy-templates:
    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: siteconfig-debug-dump
        spec:
          remediationAction: enforce
          severity: low
          object-templates-raw: |
            {{ "{{" }} $policyNamespace := "{{ $policyNamespace }}" {{ "}}" }}
            {{ "{{-" }} $configMaps := (lookup "v1" "ConfigMap" $policyNamespace "" "autoshift.io/rendered-config-map") | default dict {{ "}}" }}
            {{ "{{-" }} range $_, $cm := ($configMaps.items | default list) {{ "}}" }}
            {{ "{{-" }} $clusterName := (replace ".rendered-config" "" $cm.metadata.name) {{ "}}" }}
            {{ "{{-" }} $config := ($cm.data.config | fromYaml) {{ "}}" }}
            {{ "{{-" }} $ci := (index $config "clusterInstall" | default dict) {{ "}}" }}
            {{ "{{-" }} if eq (index $ci "createCluster" | default "") "true" {{ "}}" }}
            {{ "{{-" }} $hosts := (index $config "hosts" | default dict) {{ "}}" }}
            {{ "{{-" }} $baseDomain := (index $ci "baseDomain" | default "") {{ "}}" }}
            {{ "{{-" }} $bmcEndpoint := (index $ci "bmcEndpoint" | default "/redfish/v1/Systems/1") {{ "}}" }}
            - complianceType: mustonlyhave
              objectDefinition:
                apiVersion: v1
                kind: ConfigMap
                metadata:
                  name: siteconfig-debug-{{ "{{" }} $clusterName {{ "}}" }}
                  namespace: {{ $policyNamespace }}
                data:
                  bmh-output: |
                    {{ "{{-" }} range $hostname, $host := $hosts {{ "}}" }}
                    {{ "{{" }} $hostFQDN := (printf "%s.%s.%s" $hostname $clusterName $baseDomain) {{ "}}" }}
                    {{ "{{" }} $hostBmcEndpoint := (index $host "bmcEndpoint" | default $bmcEndpoint) {{ "}}" }}
                    {{ "{{" }} $bmcAddress := (printf "%s://%s%s" (index $host "bmcPrefix") (index $host "bmcIP") $hostBmcEndpoint) {{ "}}" }}
                    --- {{ "{{" }} $hostname {{ "}}" }} ---
                    apiVersion: metal3.io/v1alpha1
                    kind: BareMetalHost
                    metadata:
                      name: {{ "{{" }} $hostname {{ "}}" }}
                      annotations:
                        bmac.agent-install.openshift.io/hostname: {{ "{{" }} $hostFQDN {{ "}}" }}
                    spec:
                      bmc:
                        address: '{{ "{{" }} $bmcAddress {{ "}}" }}'
                        credentialsName: {{ "{{" }} $hostname {{ "}}" }}-bmc-cred
                      bootMACAddress: '{{ "{{" }} index $host "bootMACAddress" {{ "}}" }}'
                    {{ "{{-" }} if not (empty (index $host "rootDeviceHints" | default "")) {{ "}}" }}
                      rootDeviceHints:
                        {{ "{{" }} index $host "rootDeviceHints" | toYaml | autoindent {{ "}}" }}
                    {{ "{{-" }} end {{ "}}" }}
                      customDeploy:
                        method: start_assisted_install
                    {{ "{{-" }} end {{ "}}" }}
            {{ "{{-" }} end {{ "}}" }}
            {{ "{{-" }} end {{ "}}" }}
---
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: {{ $placementName }}
  namespace: {{ $policyNamespace }}
spec:
  clusterSets:
  {{ range $cluster, $value := $.Values.hubClusterSets }}
    - {{ $cluster }}
  {{ end }}
  tolerations:
    - key: cluster.open-cluster-management.io/unreachable
      operator: Exists
    - key: cluster.open-cluster-management.io/unavailable
      operator: Exists
---
apiVersion: policy.open-cluster-management.io/v1
kind: PlacementBinding
metadata:
  name: {{ $placementName }}
  namespace: {{ $policyNamespace }}
placementRef:
  name: {{ $placementName }}
  apiGroup: cluster.open-cluster-management.io
  kind: Placement
subjects:
  - name: {{ $policyName }}
    apiGroup: policy.open-cluster-management.io
    kind: Policy
{{ end }}
