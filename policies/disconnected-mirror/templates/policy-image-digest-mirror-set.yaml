# Policy to configure ImageDigestMirrorSet or ICSP for disconnected environments
# Reads mirror registry config from rendered-config ConfigMaps via hub template lookup
{{- $policyNamespace := $.Values.policy_namespace }}
apiVersion: policy.open-cluster-management.io/v1
kind: Policy
metadata:
  name: policy-disconnected-image-mirrors
  namespace: {{ $policyNamespace }}
  annotations:
    policy.open-cluster-management.io/standards: NIST SP 800-53
    policy.open-cluster-management.io/categories: CM Configuration Management
    policy.open-cluster-management.io/controls: CM-2 Baseline Configuration
spec:
{{ if (($.Values.autoshift).dryRun) }}
  remediationAction: inform
{{ end }}
  disabled: false
  policy-templates:
    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: disconnected-image-mirrors
        spec:
          remediationAction: enforce
          severity: high
          evaluationInterval:
            compliant: {{ ((($.Values.autoshift).evaluationInterval).compliant) | default "10m" }}
            noncompliant: {{ ((($.Values.autoshift).evaluationInterval).noncompliant) | default "30s" }}
          object-templates-raw: |
            {{ "{{hub" }} $cm := (lookup "v1" "ConfigMap" "{{ $policyNamespace }}" (printf "%s.rendered-config" .ManagedClusterName)) | default dict {{ "hub}}" }}
            {{ "{{hub" }} $config := (index $cm "data" | default dict) {{ "hub}}" }}
            {{ "{{hub" }} $configYaml := (index $config "config" | default "" | fromYaml | default dict) {{ "hub}}" }}
            {{ "{{hub" }} $disconnected := (index $configYaml "disconnected" | default dict) {{ "hub}}" }}
            {{ "{{hub" }} $mirror := (index $disconnected "mirrorRegistry" | default dict) {{ "hub}}" }}
            {{ "{{hub" }} $url := (index $mirror "url" | default "") {{ "hub}}" }}
            {{ "{{hub" }} $sources := (index $mirror "sources" | default list) {{ "hub}}" }}
            {{ "{{hub" }} $useIDMS := (index $disconnected "useIDMS" | default true) {{ "hub}}" }}
            {{ "{{hub" }} if (gt (len $sources) 0) {{ "hub}}" }}
            {{ "{{hub" }} if $useIDMS {{ "hub}}" }}
            - complianceType: musthave
              objectDefinition:
                apiVersion: config.openshift.io/v1
                kind: ImageDigestMirrorSet
                metadata:
                  name: autoshift-disconnected-mirrors
                spec:
                  imageDigestMirrors:
                  {{ "{{hub" }} range $_, $source := $sources {{ "hub}}" }}
                    - source: {{ "{{hub" }} $source {{ "hub}}" }}
                      mirrors:
                        - '{{ "{{hub" }} printf "%s/%s" $url $source {{ "hub}}" }}'
                  {{ "{{hub" }} end {{ "hub}}" }}
            {{ "{{hub" }} else {{ "hub}}" }}
            - complianceType: musthave
              objectDefinition:
                apiVersion: operator.openshift.io/v1alpha1
                kind: ImageContentSourcePolicy
                metadata:
                  name: autoshift-disconnected-mirrors
                spec:
                  repositoryDigestMirrors:
                  {{ "{{hub" }} range $_, $source := $sources {{ "hub}}" }}
                    - source: {{ "{{hub" }} $source {{ "hub}}" }}
                      mirrors:
                        - '{{ "{{hub" }} printf "%s/%s" $url $source {{ "hub}}" }}'
                  {{ "{{hub" }} end {{ "hub}}" }}
            {{ "{{hub" }} end {{ "hub}}" }}
            {{ "{{hub" }} end {{ "hub}}" }}
---
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: placement-policy-disconnected-image-mirrors
  namespace: {{ $policyNamespace }}
spec:
  clusterSets:
  {{- range $clusterSet, $value := .Values.managedClusterSets }}
    - {{ $clusterSet }}
  {{- end }}
  {{- range $clusterSet, $value := .Values.hubClusterSets }}
    - {{ $clusterSet }}
  {{- end }}
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchExpressions:
            - key: 'autoshift.io/disconnected-mirror'
              operator: In
              values:
              - 'true'
  tolerations:
    - key: cluster.open-cluster-management.io/unreachable
      operator: Exists
    - key: cluster.open-cluster-management.io/unavailable
      operator: Exists
---
apiVersion: policy.open-cluster-management.io/v1
kind: PlacementBinding
metadata:
  name: placement-policy-disconnected-image-mirrors
  namespace: {{ $policyNamespace }}
placementRef:
  name: placement-policy-disconnected-image-mirrors
  apiGroup: cluster.open-cluster-management.io
  kind: Placement
subjects:
  - name: policy-disconnected-image-mirrors
    apiGroup: policy.open-cluster-management.io
    kind: Policy
