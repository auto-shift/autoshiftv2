apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "AMQ-Broker-RHEL9.fullname" . }}-test-connection"
  labels:
    {{- include "AMQ-Broker-RHEL9.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "AMQ-Broker-RHEL9.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
