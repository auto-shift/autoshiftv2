# Default values for web-terminal
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
policy_namespace: open-cluster-policies
webTerminal:
  name: web-terminal
  namespace: openshift-operators
  channel: fast
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  operatorGroupName: web-terminal-operator-group
  # targetNamespaces: # Optional: specify target namespaces for namespace-scoped operators
  #   - openshift-operators
  
# hubClusterSets:
#   hub:
#     labels:
#       test1: 'test'
# managedClusterSets:
#   managed:
#     labels:
#       test4: 'test'

### AutoShift Labels Documentation
# The following labels can be set at the cluster or clusterset level to configure this policy:
#
# Enable/Disable:
# web-terminal<bool>: 'true' or 'false' - Controls whether web-terminal is managed
#
# Configuration:
# web-terminal-subscription-name<string>: Operator subscription name (default: 'web-terminal')
# web-terminal-channel<string>: Operator channel (default: 'fast')
# web-terminal-version<string>: Specific operator version (CSV) to install (optional)
# web-terminal-source<string>: Operator catalog source (default: 'redhat-operators')
# web-terminal-source-namespace<string>: Catalog namespace (default: 'openshift-marketplace')
#
# Examples:
# autoshift.io/web-terminal: 'true'
# autoshift.io/web-terminal-subscription-name: 'web-terminal'
# autoshift.io/web-terminal-channel: 'fast'
# autoshift.io/web-terminal-version: 'operator-name.v1.x.x'
# autoshift.io/web-terminal-source: 'redhat-operators'
# autoshift.io/web-terminal-source-namespace: 'openshift-marketplace'