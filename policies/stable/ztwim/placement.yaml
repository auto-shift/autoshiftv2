apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  # Shared by every ztwim policy except federation, so it is named for the component rather than for
  # one policy in it.
  name: placement-policy-ztwim
  namespace: ${POLICY_NAMESPACE}
spec:
  # No spec.clusterSets: the Placement selects across all ManagedClusterSetBindings in this
  # namespace (the top autoshift chart binds every clusterset here) filtered by the predicate.
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchExpressions:
            - key: 'autoshift.io/ztwim'
              operator: In
              values:
                - 'true'
  tolerations:
    - key: cluster.open-cluster-management.io/unreachable
      operator: Exists
    - key: cluster.open-cluster-management.io/unavailable
      operator: Exists
