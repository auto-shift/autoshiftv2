apiVersion: policy.open-cluster-management.io/v1
kind: PolicyGenerator
metadata:
  name: cert-manager
# Per-deployment scalars (${...}) are substituted by the repo-server CMP (envsubst/sed) BEFORE
# `kustomize build` runs. They live here and in placement.yaml — never in manifests/, whose hub
# templates contain `$vars` that substitution would clobber.
#
# Each manifests[].path is a DIRECTORY wildcard: drop a manifest into manifests/operator-install/
# (operator) or manifests/ca/ (the autoshift-ca issuer) and it is picked up automatically. Only edit
# this file to change the policy graph itself (names, dependencies, per-policy remediation). Keep
# placement.yaml OUT of the wildcarded dirs (a Placement there would be wrapped into the Policy).
placementBindingDefaults:
  name: binding-policy-cert-manager
policyDefaults:
  namespace: ${POLICY_NAMESPACE}
  # ${REMEDIATION} = inform when autoshift.dryRun, else enforce.
  remediationAction: ${REMEDIATION}
  severity: high
  # Consolidate the raw Namespace into one generated ConfigurationPolicy; the first-class
  # OperatorPolicy stays its own policy-template. Net: one Policy, two policy-templates.
  consolidateManifests: true
  evaluationInterval:
    compliant: ${EVAL_COMPLIANT}
    noncompliant: ${EVAL_NONCOMPLIANT}
  standards:
    - NIST SP 800-53
  categories:
    - CM Configuration Management
  controls:
    - CM-2 Baseline Configuration
  # Placement authored by us (placement.yaml at the dir root, full fidelity); PG includes it and
  # generates only the PlacementBinding.
  placement:
    placementPath: placement.yaml
policies:
  - name: policy-cert-manager-operator-install
    manifests:
      - path: manifests/operator-install
  # AutoShift's built-in self-signed CA (autoshift-ca ClusterIssuer) — the default TLS issuer for clusters
  # without an external ACME issuer. Needs the operator (CRDs) installed first.
  - name: policy-cert-manager-ca
    dependencies:
      - name: policy-cert-manager-operator-install
    manifests:
      - path: manifests/ca
