# Default values for cluster-labels.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
# Labels can be set from Application Set
---
debug: false
enabled: true
autoshiftLabelPrefix: "autoshift.io/"
policy_namespace: open-cluster-policies
# hubClusterSets:
#   hub:
#     labels: 
#       # infra-nodes-numcpu: '8'
#       # infra-nodes-memory-mib:  '24576'
#       # infra-nodes-numcores-per-socket:  '4'
#       # infra-nodes:  '6'
#       # worker-nodes-numcpu:  '8'
#       # worker-nodes-memory-mib:  '32768'
#       # worker-nodes-numcores-per-socket:  '4'
#       # worker-nodes:  '3'
#       # test-worked: 'false'
# managedClusterSets:
#   managed:
#     labels: 
#       # infra-nodes-numcpu: '8'
#       # infra-nodes-memory-mib:  '24576'
#       # infra-nodes-numcores-per-socket:  '4'
#       # infra-nodes:  '6'
#       # worker-nodes-numcpu:  '8'
#       # worker-nodes-memory-mib:  '32768'
#       # worker-nodes-numcores-per-socket:  '4'
#       # worker-nodes:  '3'
# clusters:
#   nonprod:
#     labels:
#       # infra-nodes-numcpu: '8'
#       # infra-nodes-memory-mib:  '24576'
#       # infra-nodes-numcores-per-socket:  '4'
#       # infra-nodes:  '6'
#       # worker-nodes-numcpu:  '8'
#       # worker-nodes-memory-mib:  '32768'
#       # worker-nodes-numcores-per-socket:  '4'
#       # worker-nodes:  '3'
#   local-cluster: 
#     labels: 
#       # test-worked: 'true'
#       # second-label: ''

### AutoShift Labels Documentation
# This policy manages the AutoShift label system itself.
# It automatically applies labels from cluster sets to individual clusters.
#
# System Configuration:
# This policy is always enabled and manages all autoshift.io/* labels
# Label precedence: cluster labels > clusterset labels > helm values
#
# Derived system labels (set by this policy, never from values):
# - autoshift.io/owning-namespace / autoshift.io/owning-deployment: the deployment that owns
#   the cluster's clusterset config.
# - autoshift.io/cluster-type: selfManagedHub | managedHub | spoke. Derived at runtime from the
#   cluster-set-type data key this chart writes into every cluster-set.<name> ConfigMap (hub
#   for hubClusterSets, spoke for managedClusterSets, the type field for clusterSets); hub-set
#   members split into selfManagedHub/managedHub on the set's self-managed label. Works across
#   deployments because the policy's ConfigMap lookup is cluster-wide and label-selected, so it
#   sees every deployment's CMs.
#
# Note: This policy references various labels in debug template but doesn't 
# directly consume them - it manages the label propagation system.
#
# The policy ensures labels defined at clusterset level are propagated to
# member clusters, while allowing cluster-specific overrides.
