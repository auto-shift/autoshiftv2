# MIG strategy per-cluster (none | single | mixed), resolved from a label at ACM enforcement time.
apiVersion: nvidia.com/v1
kind: ClusterPolicy
metadata:
  name: gpu-cluster-policy
spec:
  mig:
    strategy: '{{hub index .ManagedClusterLabels "autoshift.io/gpu-mig-strategy" | default "single" hub}}'
