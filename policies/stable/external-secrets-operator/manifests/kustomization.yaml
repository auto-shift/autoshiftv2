apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Renders the shared components/operator-install chart. Visible per-operator ACM lookups below.
helmGlobals:
  chartHome: ../../../../components/
helmCharts:
  - name: operator-install
    releaseName: external-secrets-operator
    valuesInline:
      name: external-secrets-operator
      namespace: external-secrets-operator
      remediation: ${REMEDIATION}
      subscription:
        name: '{{hub index .ManagedClusterLabels "autoshift.io/external-secrets-operator-subscription-name" | default "openshift-external-secrets-operator" hub}}'
        channel: '{{hub index .ManagedClusterLabels "autoshift.io/external-secrets-operator-channel" | default "stable-v1" hub}}'
        source: '{{hub $base := (index .ManagedClusterLabels "autoshift.io/external-secrets-operator-source" | default "redhat-operators") hub}}{{hub ternary (printf "%s-%s" $base (index .ManagedClusterLabels "autoshift.io/mirror-catalog-suffix" | default "mirror")) $base (eq (index .ManagedClusterLabels "autoshift.io/disconnected-mirror" | default "false") "true") hub}}'
        sourceNamespace: '{{hub index .ManagedClusterLabels "autoshift.io/external-secrets-operator-source-namespace" | default "openshift-marketplace" hub}}'
        startingCSV: '{{hub index .ManagedClusterLabels "autoshift.io/external-secrets-operator-version" | default "" hub}}'
      versions: '{{hub compact (list (index .ManagedClusterLabels "autoshift.io/external-secrets-operator-version" | default "")) | toRawJson | toLiteral hub}}'
